# Cursor Adapter

Cursor should consume generated rule output from
`harness/templates/cursor-rules.template`.

Keep Cursor-specific formatting and file locations in this adapter/template.
Core rules stay under `harness/core`.
